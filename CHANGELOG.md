# Change Log

## [Unreleased]

- Add VS Code command and context menu for running business-project-local `gen-api-types`.
- Add configuration mapping for `project_root`, `output_file`, `output_dir`, `ts_config_path`, and `isExported`.
- Generate output to the current TypeScript file directory by default.
- Confirm before overwriting an existing generated type file.
- Replace template README with `gen-api-types` extension usage documentation.
